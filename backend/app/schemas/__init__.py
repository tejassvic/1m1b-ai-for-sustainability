"""Request/response contracts for the Verdant API.

Input validation lives here so the service layer can trust what it receives —
validation is part of the responsible-AI surface, not an afterthought.
"""
